const site_url = process.env.NEXT_PUBLIC_SITE_URL;
const backend_url = process.env.NEXT_PUBLIC_BACKEND_URL;
const spaces_url  = process.env.NEXT_PUBLIC_SPACES_URL; 

export const Env = { site_url, backend_url, spaces_url };
